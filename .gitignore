# --- General ---
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- Node.js (Your App) ---
node_modules/
build/
npm-debug.log
yarn-error.log
.env
dist/
coverage/

# --- Docker ---
# We generally commit Dockerfiles, but ignore local overrides
docker-compose.override.yml

# --- Terraform (Infrastructure) ---
# .terraform is where plugins/providers are downloaded (heavy)
.terraform/
# State files contain sensitive info about your infrastructure
*.tfstate
*.tfstate.backup
# Variable files often contain passwords/keys
*.tfvars
.terraform.lock.h
runbook/

# --- Ansible (Configuration) ---
*.retry
# Ignore the inventory if it's auto-generated by Terraform
inventory.ini
ansible.log

# --- AWS & Security (CRITICAL) ---
# Never commit your SSH keys or AWS credentials!
*.pem
*.ppk
*.key
.aws/
creds.txt

assignment.*